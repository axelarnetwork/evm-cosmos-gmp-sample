{
  "name": "swap-and-forward",
  "version": "1.0.0",
  "description": "performs a swap on Osmosis and forward the token to destination chain",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf artifacts && hardhat compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@axelar-network/axelar-gmp-sdk-solidity": "^2.0.0",
    "@openzeppelin/contracts": "^4.8.1"
  },
  "devDependencies": {
    "config": "^3.3.8",
    "hardhat": "^2.12.2"
  }
}
